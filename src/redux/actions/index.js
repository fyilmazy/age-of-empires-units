import increase from "./increaseTest";
import decrease from "./decreaseTest";
import logSaga from "./logSaga";

const actions = { increase, decrease, logSaga };

export { actions as default, increase, decrease, logSaga };
